Applestoo.Views.PointTotalsIndex = Backbone.View.extend({

  // el: "#my_points",
  // template: JST['templates/point_totals/show'],
  // render: function() {
  //   this.$el.html( this.template() );
  // }
});
